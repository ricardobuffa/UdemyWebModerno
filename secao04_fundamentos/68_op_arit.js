//Aritiméticos são operadores binários
const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;

//operando postfix --> d++
//operando prefix --> ++d
//operando infix --> op1 + op2

const subtracao = d - b;

const multiplicacao = a * b;

const divisao = d /a;

const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo)